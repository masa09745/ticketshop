- @stocks.each do |stock|
  .match-ticket__inner
    .match-ticket__inner--upr
      .ticket-grade
        = stock.grade
      .ticket-price
        =stock.price
        円
    .match-ticket__inner--lwr
      =link_to stock_orders_path(@stock) do
        枚数を選ぶ