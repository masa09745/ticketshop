= render "shared/header_main"
.buy-wrapper
  .buy-inner
    .buy-inner__top
      戻る
    .buy-inner__main
      .buy-title
        チケット購入ページ
      .buy-form
        .buy-form__inner
          .match-name
            %label 試合名
            %p
              = @schedule.match_name
          .match-venue
            %label 会場
            %p Aスタジアム
          .match-date
            %label 試合日
            %p
              = @schedule.match_date.strftime('%m月 %d日')
          .match-time
            %label 開始
            %p
              = @schedule.match_date.strftime('%H:%M')
          .seat-typebox
            = form_with(model:[@schedule,@order], class: "seat-type")do |f|
              .seat-type__ss
                SS席 :seat_type
              .order-count
                = f.text_field :ticket_count
              .buy-button
                =f.submit "購入する", class:"waves-effect waves-light btn-large"
            = form_with(model:[@schedule,@order], class: "seat-type")do |f|
              .seat-type__ss
                %label SS席
              .order-count
                = f.text_field :ticket_count
              .buy-button
                =f.submit "購入する", class:"waves-effect waves-light btn-large"
            = form_with(model:[@schedule,@order], class: "seat-type")do |f|
              .seat-type__ss
                %label SS席
              .order-count
                = f.text_field :ticket_count
              .buy-button
                =f.submit "購入する", class:"waves-effect waves-light btn-large"
            = form_with(model:[@schedule,@order], class: "seat-type")do |f|
              .seat-type__ss
                %label SS席
              .order-count
                = f.text_field :ticket_count
              .buy-button
                =f.submit "購入する", class:"waves-effect waves-light btn-large"