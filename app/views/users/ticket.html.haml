.mypage-wrapper
  = render "/shared/header-main"
  = render "/users/shared/user-mypage"
  .mypage-ticket
    -@tickets.each do |ticket|
      .hoge
        =ticket.grade
      .hogehoge
        =ticket.price
      .hoge
        =ticket.schedule.match_date
      .hoge
        =ticket.schedule.venue.name
  = render "/shared/footer-main"

